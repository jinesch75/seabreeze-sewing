# Dependencies
node_modules/

# Uploaded photos (these live on the server — not tracked in git)
public/images/uploads/

# Data files — these are managed live on the server (admin uploads, contacts,
# settings, and editable page content). They must NOT be overwritten when you
# pull code changes from GitHub.
data/settings.json
data/designs.json
data/contacts.json
data/content.json

# OS / editor junk
.DS_Store
Thumbs.db
*.log
