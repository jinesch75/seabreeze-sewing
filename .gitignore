# Dependencies
node_modules/

# Uploaded photos (these live on the server — not tracked in git)
public/images/uploads/

# Data files — these are managed live on the server (admin uploads, contacts,
# settings). They must NOT be overwritten when you pull code changes from GitHub.
data/settings.json
data/designs.json
data/contacts.json

# OS / editor junk
.DS_Store
Thumbs.db
*.log
